#include <stdio.h>


int main()
{

  // operators in c
  // 15 * 30 --- arithmetic
  // 45 >= 43 --- relational
  // true && false -- logical
  // 

  /* 
  
  -arithmetic (+ - * / %)
  -increment/decrement( -- ++ )
  -relational (< > <= >= != ==)
  -logical ( && || !)
  -bitwise (& ^ | ~ >> <<)
  -assignment (= += -= *= /= %= <<= >>= &= |= ^=)
  -tarnary/conditional ( ?:)
  -address-of (&)
  -pointer (*)
  -sizeof()
  -comma op(,)

   */

  
  
  return 0;
}

